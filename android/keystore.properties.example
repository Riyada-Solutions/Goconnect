# Copy to android/keystore.properties (gitignored) and fill in the real values.
# Retrieve them with:  eas credentials -p android   →  Keystore → download / show
#
# storeFile is resolved relative to android/app/.
storeFile=release.keystore
storePassword=
keyAlias=
keyPassword=
